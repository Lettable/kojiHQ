/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ibb.co', 'pbs.twimg.com', 'images.unsplash.com', 'imgur.com', 's13.gifyu.com', 'i.imgur.com'],
  }
};

export default nextConfig;
